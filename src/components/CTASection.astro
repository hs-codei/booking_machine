---
import { siteConfig } from '../../site.config';
---

<section class="cta-section section">
  <div class="container">
    <div class="cta-content animate-on-scroll">
      <h2>Bereit für den ersten Schritt?</h2>
      <p>
        Vereinbaren Sie jetzt Ihren Termin in unserer Praxis in {siteConfig.address.district}.
        Wir freuen uns, Sie auf Ihrem Weg zu mehr Beweglichkeit zu begleiten.
      </p>
      <div class="cta-actions">
        <a href="/termin" class="btn btn-accent">Online Termin buchen</a>
        <a href={`tel:${siteConfig.phoneTel}`} class="btn btn-white">
          <svg viewBox="0 0 24 24"><path d="M6.62 10.79a15.053 15.053 0 006.59 6.59l2.2-2.2a1.003 1.003 0 011.01-.24c1.12.37 2.33.57 3.57.57.55 0 1 .45 1 1V20c0 .55-.45 1-1 1-9.39 0-17-7.61-17-17 0-.55.45-1 1-1h3.5c.55 0 1 .45 1 1 0 1.25.2 2.45.57 3.57.1.31.03.66-.25 1.02l-2.2 2.2z"/></svg>
          {siteConfig.phone}
        </a>
      </div>
      <p class="cta-note">Alle Kassen · Termine oft noch diese Woche verfügbar</p>
    </div>
  </div>
</section>

<style>
  .cta-section {
    background: linear-gradient(135deg, var(--color-primary-dark) 0%, var(--color-primary) 100%);
    color: white;
    text-align: center;
    padding: clamp(3rem, 8vw, 5rem) 0;
  }

  .cta-content {
    max-width: 600px;
    margin: 0 auto;
  }

  .cta-section h2 {
    font-family: var(--font-display);
    font-size: clamp(1.75rem, 4vw, 2.5rem);
    margin-bottom: 1rem;
    color: white;
  }

  .cta-section p {
    font-size: 1.1rem;
    opacity: 0.9;
    margin-bottom: 2rem;
    line-height: 1.6;
  }

  .cta-actions {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
    margin-bottom: 1.5rem;
  }

  .btn-white {
    background: white;
    color: var(--color-primary-dark);
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
  }

  .btn-white:hover {
    background: var(--color-bg);
    transform: translateY(-2px);
  }

  .btn-white svg {
    width: 18px;
    height: 18px;
    fill: currentColor;
  }

  .cta-note {
    font-size: 0.85rem;
    opacity: 0.75;
    margin-bottom: 0;
  }
</style>
