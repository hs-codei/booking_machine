---
import BaseLayout from '../../layouts/BaseLayout.astro';
import TopBar from '../../components/TopBar.astro';
import Header from '../../components/Header.astro';
import MobileCTA from '../../components/MobileCTA.astro';
import Footer from '../../components/Footer.astro';
import { siteConfig } from '../../../site.config';

// Page-specific SEO
const pageTitle = `Rückenschmerzen behandeln — Physiotherapie ${siteConfig.address.district}`;
const pageDescription = `Effektive Behandlung von Rückenschmerzen in ${siteConfig.address.city}-${siteConfig.address.district}. Manuelle Therapie & Krankengymnastik bei ${siteConfig.name}. Jetzt Termin vereinbaren.`;

// FAQ Schema
const faqSchema = {
  "@context": "https://schema.org",
  "@type": "FAQPage",
  "mainEntity": [
    {
      "@type": "Question",
      "name": "Wie lange dauert eine Behandlung bei Rückenschmerzen?",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "Die Dauer der Behandlung hängt von der Ursache und Schwere der Beschwerden ab. In der Regel sehen Patienten nach 6-10 Sitzungen deutliche Verbesserungen. Bei akuten Schmerzen kann oft schon nach 2-3 Behandlungen eine Linderung eintreten."
      }
    },
    {
      "@type": "Question",
      "name": "Zahlt die Krankenkasse Physiotherapie bei Rückenschmerzen?",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "Ja, mit einer ärztlichen Verordnung übernehmen alle gesetzlichen Krankenkassen die Kosten für Physiotherapie. Es fällt lediglich eine Zuzahlung von 10% plus 10 Euro Verordnungsgebühr an. Privat Versicherte erhalten in der Regel eine vollständige Erstattung."
      }
    },
    {
      "@type": "Question",
      "name": "Welche Behandlungsmethoden helfen bei Rückenschmerzen?",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "Je nach Ursache setzen wir manuelle Therapie, Krankengymnastik, Wärmetherapie und gezielte Übungen ein. Bei muskulären Verspannungen hilft oft die Kombination aus manueller Behandlung und aktiven Übungen am besten."
      }
    }
  ]
};
---

<BaseLayout
  title={pageTitle}
  description={pageDescription}
>
  <MobileCTA />
  <TopBar />
  <Header />

  <main>
    <!-- Hero Section -->
    <section class="service-hero section">
      <div class="container">
        <div class="service-hero-content animate-on-scroll">
          <div class="breadcrumb">
            <a href="/">Startseite</a> / <a href="/#leistungen">Leistungen</a> / Rückenschmerzen
          </div>

          <h1>Rückenschmerzen behandeln in {siteConfig.address.district}</h1>

          <p class="service-intro">
            Rückenschmerzen gehören zu den häufigsten Beschwerden in Deutschland. In unserer Praxis
            behandeln wir täglich Patienten mit akuten und chronischen Rückenproblemen — mit
            individuellen Therapieplänen und bewährten Methoden.
          </p>

          <div class="service-cta-group">
            <a href="/termin" class="btn btn-primary">Termin buchen</a>
            <a href={`tel:${siteConfig.phoneTel}`} class="btn btn-secondary">{siteConfig.phone}</a>
          </div>
        </div>
      </div>
    </section>

    <!-- Content Section -->
    <section class="service-content section section--alt">
      <div class="container">
        <div class="content-grid">
          <div class="content-main">
            <h2>Ursachen von Rückenschmerzen</h2>
            <p>
              Rückenschmerzen können viele Ursachen haben: muskuläre Verspannungen durch langes Sitzen,
              Bandscheibenprobleme, Fehlhaltungen oder Überlastung im Alltag. Oft ist es eine Kombination
              aus mehreren Faktoren.
            </p>

            <h3>Typische Auslöser:</h3>
            <ul>
              <li>Langes Sitzen am Arbeitsplatz</li>
              <li>Einseitige Belastungen</li>
              <li>Bewegungsmangel</li>
              <li>Stress und Anspannung</li>
              <li>Fehlhaltungen</li>
              <li>Nach Unfällen oder Verletzungen</li>
            </ul>

            <h2>Unsere Behandlung</h2>
            <p>
              Nach einer gründlichen Befundaufnahme erstellen wir einen individuellen Therapieplan.
              Je nach Ursache setzen wir verschiedene Behandlungsmethoden ein:
            </p>

            <div class="treatment-cards">
              <div class="treatment-card">
                <h4>Manuelle Therapie</h4>
                <p>Gezielte Handgriffe lösen Blockaden und Verspannungen in der Wirbelsäule.</p>
              </div>
              <div class="treatment-card">
                <h4>Krankengymnastik</h4>
                <p>Aktive Übungen stärken die Rückenmuskulatur und verbessern die Haltung.</p>
              </div>
              <div class="treatment-card">
                <h4>Wärmetherapie</h4>
                <p>Fango und Heißluft lockern verspannte Muskeln vor der manuellen Behandlung.</p>
              </div>
            </div>

            <h2>Was Sie erwartet</h2>
            <ol>
              <li><strong>Erstgespräch:</strong> Wir besprechen Ihre Beschwerden und führen eine körperliche Untersuchung durch.</li>
              <li><strong>Diagnose:</strong> Basierend auf der Untersuchung identifizieren wir die Ursache Ihrer Schmerzen.</li>
              <li><strong>Therapieplan:</strong> Wir erstellen einen individuellen Plan mit realistischen Zielen.</li>
              <li><strong>Behandlung:</strong> In regelmäßigen Sitzungen arbeiten wir an Ihrer Beschwerdefreiheit.</li>
              <li><strong>Übungen für zu Hause:</strong> Sie erhalten Übungen, die Sie selbstständig durchführen können.</li>
            </ol>
          </div>

          <aside class="content-sidebar">
            <div class="sidebar-card">
              <h3>Schnelle Hilfe?</h3>
              <p>Bei akuten Rückenschmerzen versuchen wir, Ihnen zeitnah einen Termin anzubieten.</p>
              <a href={`tel:${siteConfig.phoneTel}`} class="btn btn-primary">{siteConfig.phone}</a>
            </div>

            <div class="sidebar-card">
              <h3>Kostenübernahme</h3>
              <p>Mit ärztlicher Verordnung übernehmen alle Kassen die Behandlungskosten.</p>
              <ul>
                <li>Alle gesetzlichen Kassen</li>
                <li>Private Krankenversicherung</li>
                <li>Berufsgenossenschaft</li>
              </ul>
            </div>

            <div class="sidebar-card sidebar-card--trust">
              <div class="trust-badge">
                <span class="stars">★★★★★</span>
                <span>{siteConfig.rating} von 5</span>
              </div>
              <p>{siteConfig.reviewCount} zufriedene Patienten auf Google</p>
            </div>
          </aside>
        </div>
      </div>
    </section>

    <!-- FAQ Section -->
    <section class="faq-section section">
      <div class="container">
        <h2 class="section-title text-center">Häufige Fragen</h2>

        <div class="faq-list">
          {faqSchema.mainEntity.map((faq) => (
            <details class="faq-item">
              <summary>{faq.name}</summary>
              <p>{faq.acceptedAnswer.text}</p>
            </details>
          ))}
        </div>
      </div>
    </section>

    <!-- CTA Section -->
    <section class="cta-section section section--dark">
      <div class="container text-center">
        <h2>Bereit, Ihre Rückenschmerzen loszuwerden?</h2>
        <p>Vereinbaren Sie jetzt einen Termin in unserer Praxis in {siteConfig.address.district}.</p>
        <div class="cta-buttons">
          <a href="/termin" class="btn btn-accent">Online Termin buchen</a>
          <a href={`tel:${siteConfig.phoneTel}`} class="btn btn-secondary">{siteConfig.phone}</a>
        </div>
      </div>
    </section>
  </main>

  <Footer />

  <!-- FAQ Schema -->
  <script type="application/ld+json" set:html={JSON.stringify(faqSchema)} />
</BaseLayout>

<style>
  .service-hero {
    background: linear-gradient(165deg, var(--color-bg) 0%, var(--color-bg-alt) 100%);
  }

  .breadcrumb {
    font-size: 0.85rem;
    color: var(--color-text-light);
    margin-bottom: 1rem;
  }

  .breadcrumb a {
    color: var(--color-primary);
  }

  .service-hero h1 {
    font-family: var(--font-display);
    font-size: clamp(2rem, 5vw, 3rem);
    color: var(--color-primary-dark);
    margin-bottom: 1rem;
    max-width: 700px;
  }

  .service-intro {
    font-size: 1.1rem;
    color: var(--color-text-light);
    max-width: 600px;
    margin-bottom: 2rem;
  }

  .service-cta-group {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
  }

  /* Content Grid */
  .content-grid {
    display: grid;
    grid-template-columns: 1fr 350px;
    gap: 3rem;
    align-items: start;
  }

  .content-main h2 {
    font-family: var(--font-display);
    font-size: 1.6rem;
    color: var(--color-primary-dark);
    margin-top: 2rem;
    margin-bottom: 1rem;
  }

  .content-main h2:first-child {
    margin-top: 0;
  }

  .content-main h3 {
    font-size: 1.1rem;
    font-weight: 600;
    margin-top: 1.5rem;
    margin-bottom: 0.75rem;
  }

  .content-main p {
    color: var(--color-text);
    margin-bottom: 1rem;
    line-height: 1.7;
  }

  .content-main ul, .content-main ol {
    margin-bottom: 1.5rem;
    padding-left: 1.5rem;
  }

  .content-main li {
    margin-bottom: 0.5rem;
    color: var(--color-text);
  }

  /* Treatment Cards */
  .treatment-cards {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 1rem;
    margin: 1.5rem 0;
  }

  .treatment-card {
    background: var(--color-white);
    border: 1px solid var(--color-border);
    border-radius: var(--border-radius);
    padding: 1.25rem;
  }

  .treatment-card h4 {
    font-family: var(--font-display);
    font-size: 1rem;
    color: var(--color-primary);
    margin-bottom: 0.5rem;
  }

  .treatment-card p {
    font-size: 0.9rem;
    margin: 0;
  }

  /* Sidebar */
  .content-sidebar {
    position: sticky;
    top: 100px;
  }

  .sidebar-card {
    background: var(--color-white);
    border: 1px solid var(--color-border);
    border-radius: var(--border-radius);
    padding: 1.5rem;
    margin-bottom: 1rem;
  }

  .sidebar-card h3 {
    font-family: var(--font-display);
    font-size: 1.1rem;
    color: var(--color-primary-dark);
    margin-bottom: 0.75rem;
  }

  .sidebar-card p {
    font-size: 0.9rem;
    color: var(--color-text-light);
    margin-bottom: 1rem;
  }

  .sidebar-card ul {
    list-style: none;
    padding: 0;
    margin: 0;
    font-size: 0.9rem;
  }

  .sidebar-card li {
    padding: 0.25rem 0;
    color: var(--color-text);
  }

  .sidebar-card li::before {
    content: '✓ ';
    color: var(--color-primary);
  }

  .sidebar-card--trust {
    background: var(--color-primary);
    color: var(--color-white);
    text-align: center;
  }

  .sidebar-card--trust p {
    color: rgba(255, 255, 255, 0.8);
    margin: 0;
  }

  .trust-badge {
    display: flex;
    justify-content: center;
    gap: 0.5rem;
    font-size: 1.1rem;
    font-weight: 700;
    margin-bottom: 0.5rem;
  }

  /* FAQ */
  .faq-list {
    max-width: 700px;
    margin: 0 auto;
  }

  .faq-item {
    background: var(--color-white);
    border: 1px solid var(--color-border);
    border-radius: var(--border-radius);
    margin-bottom: 0.75rem;
    overflow: hidden;
  }

  .faq-item summary {
    padding: 1.25rem;
    cursor: pointer;
    font-weight: 600;
    color: var(--color-text);
    list-style: none;
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .faq-item summary::after {
    content: '+';
    font-size: 1.5rem;
    color: var(--color-primary);
  }

  .faq-item[open] summary::after {
    content: '−';
  }

  .faq-item p {
    padding: 0 1.25rem 1.25rem;
    color: var(--color-text-light);
    margin: 0;
  }

  /* CTA Section */
  .cta-section h2 {
    color: var(--color-white);
    margin-bottom: 0.75rem;
  }

  .cta-section p {
    opacity: 0.85;
    margin-bottom: 1.5rem;
  }

  .cta-buttons {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
  }

  @media (max-width: 900px) {
    .content-grid {
      grid-template-columns: 1fr;
    }

    .content-sidebar {
      position: static;
    }
  }
</style>
