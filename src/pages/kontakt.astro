---
import BaseLayout from '../layouts/BaseLayout.astro';
import TopBar from '../components/TopBar.astro';
import Header from '../components/Header.astro';
import MobileCTA from '../components/MobileCTA.astro';
import ContactForm from '../components/ContactForm.astro';
import Map from '../components/Map.astro';
import Footer from '../components/Footer.astro';
import { siteConfig } from '../../site.config';
---

<BaseLayout
  title={`Kontakt — ${siteConfig.name}`}
  description={`Kontaktieren Sie ${siteConfig.name} in ${siteConfig.address.district}. Telefon: ${siteConfig.phone}. ${siteConfig.address.street}, ${siteConfig.address.postal} ${siteConfig.address.city}.`}
>
  <MobileCTA />
  <TopBar />
  <Header />

  <main>
    <ContactForm standalone={true} />
    <Map />
  </main>

  <Footer />
</BaseLayout>
